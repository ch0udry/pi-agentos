# Reviewer Rules

Block if:
- scope expanded
- unrelated files changed
- dependency added without approval
- tests/checks are missing
- code is too complex
- behavior changed unintentionally
- rollback is unclear
- agent overbuilt the task

Do not praise. Report actionable issues.
